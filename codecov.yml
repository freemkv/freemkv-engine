# Coverage is INFORMATIONAL only — it never fails a check or blocks a merge.
coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

# No PR comment noise.
comment: false

# Not product code / not meaningfully coverable.
ignore:
  - "tests/**"
  - "ci/**"
